#!/bin/sh

# A (rofi) dmenu wrapper script for system functions.
case $(dmenu -i -p 'Action: ' << EOF
󰌾 lock
󰍃 logout
󰤄 sleep
 reboot
󰐥 shutdown
EOF
) in
    '󰌾 lock') loginctl lock-session;;
    '󰍃 logout') i3-msg exit ;;
    '󰤄 sleep') systemctl suspend ;;
    ' reboot') systemctl reboot ;;
    '󰐥 shutdown') systemctl poweroff ;;
    *) exit 1 ;;
esac
